name: "test2"
on:
  push:
    branches:
      - "main"
  workflow_dispatch:
    
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: "push message"
        run: bash ./test2.sh
        